name: CI

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "step 1"
      - uses: actions/checkout@v4
      - run: echo "step 3 checkout done"
